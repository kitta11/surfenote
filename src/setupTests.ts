import '@testing-library/jest-dom'
//This will extend Jest's matchers with @testing-library/jest-dom's matchers (including toBeInTheDocument).
console.log('SetupTests is running') // Add this line to confirm execution
